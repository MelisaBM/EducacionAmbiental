<style>
        body {
            background-color: #f4f4f4;
            color: #333;
            text-align: center;
        }

        header {
            background-color: #2c7a7b;
            color: #fff;
            padding: 20px 0;
        }

        header h1 {
            margin: 0;
            font-size: 2.5rem;
        }

        header p {
            font-size: 1.2rem;
        }

        .gallery-item {
            position: relative;
            overflow: hidden;
            cursor: pointer;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
            width: 200px; /* Ajusta según el tamaño deseado */
    height: 200px; /* Mantén el contenedor cuadrado */
            margin: 1; /* Reduce el margen para acercar los elementos */
        }

        .gallery-item img, .gallery-item video {
            width: 100%;
            height: 100%;
            display: block;
             max-width: 200px;
            object-fit: cover;
        }

        .gallery-item:hover {
            transform: scale(1.05);
        }
        .gallery-container {
    padding: 0; /* Elimina cualquier padding en el contenedor principal */
    display: flex;
    flex-wrap: wrap;
    gap: 5px; /* Si estás utilizando flexbox o grid, controla el espacio con gap */
}
.row {
    margin: 1; /* Elimina el margen de la fila */
}

.col-md-4 {
    padding: 0; /* Elimina el padding de las columnas */
}

    </style>
